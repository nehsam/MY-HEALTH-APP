# --- core ---
streamlit==1.34.0          # UI framework
pydantic>=2.0.0            # data models
python-dotenv==1.0.1       # .env loader

# --- async / mqtt (if really needed) ---
asyncio-mqtt>=0.13.0

# --- typing helper ---
typing-extensions>=4.0.0

# --- date/time helper (PyPI package, *not* std‑lib) ---
DateTime>=5.5              # ← note the capital D • standard‑lib “datetime” is *already* built‑in

# --- data / viz ---
pandas>=2.0.0
plotly>=5.0.0

# --- LLM / PDF libs ---
groq==0.1.5
fpdf==1.7.2
google-generativeai==0.4.1
